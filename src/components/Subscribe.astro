---
// StaticForms capture (shared portfolio key; client-side by design). _site tag = source attribution.
const accessKey = 'sf_9e906eb6c00416b9d3354749';
---
<section id="subscribe" class="wrap-narrow my-20 scroll-mt-24">
  <div class="rounded-3xl bg-ink text-paper p-8 sm:p-12 text-center">
    <h2 class="font-display text-3xl font-extrabold">Get 10 validated ideas every week</h2>
    <p class="mt-3 text-paper/70 max-w-xl mx-auto">
      One email, every Friday: fresh startup and side-project ideas with demand signals and a starter stack. No spam, unsubscribe in one click.
    </p>
    <form action="https://api.staticforms.xyz/submit" method="POST" class="mt-7 flex flex-col sm:flex-row gap-3 max-w-md mx-auto">
      <input type="hidden" name="accessKey" value={accessKey} />
      <input type="hidden" name="subject" value="ideeeas.com, new subscriber" />
      <input type="hidden" name="_site" value="ideeeas.com" />
      <input type="text" name="honeypot" style="display:none" tabindex="-1" autocomplete="off" />
      <input
        type="email"
        name="email"
        required
        placeholder="you@email.com"
        class="flex-1 rounded-full px-5 py-3 text-ink bg-paper placeholder:text-mist focus:outline-none focus:ring-2 focus:ring-spark"
      />
      <button type="submit" class="rounded-full bg-spark text-ink font-semibold px-6 py-3 hover:bg-paper transition-colors">
        Subscribe free
      </button>
    </form>
  </div>
</section>
